import { Tabs } from "nextra/components";
import Calendar from "@start-base/react-form-elements/Calendar";
import "@start-base/react-form-elements/styles.css";
import "react-day-picker/dist/style.css";
import Preview from "../../components/Preview/Preview";
import ApiReferenceButton from "../../components/ApiReferenceButton/ApiReferenceButton";
import { Steps } from "nextra/components";
import FormOnChange from "./formOnChange";

# Calendar

A calendar component that shows date as a picker

<ApiReferenceButton path="form-calendar--docs" />

## Installation

<Steps>

### Install the package

<Tabs items={["npm", "yarn", "pnpm"]}>
  <Tabs.Tab>
    ``` bash copy npm install --save @start-base/react-form-elements ```
  </Tabs.Tab>
  <Tabs.Tab>
    ``` bash copy yarn add @start-base/react-form-elements ```
  </Tabs.Tab>
  <Tabs.Tab>
    ``` bash copy pnpm add @start-base/react-form-elements ```
  </Tabs.Tab>
</Tabs>
### Add the styles Make sure to add css file to your app root file

```tsx copy
import "@start-base/react-form-elements/styles.css";
```

</Steps>

<Tabs items={["Preview", "Code"]}>
  <Tabs.Tab>
    <Preview height="500px">
     
    <FormOnChange>
     <Calendar
        label="Label"
        name="input"
    />
  </FormOnChange>
   </Preview>
  </Tabs.Tab>
  <Tabs.Tab>
    ```ts copy showLineNumbers
    import Checkbox from '@start-base/react-form-elements/Checkbox';
    import '@start-base/react-form-elements/styles.css';

    <Calendar
      label="Label"
      name="input"
      onChange={() => {}}
    />
    ```

  </Tabs.Tab>
</Tabs>
