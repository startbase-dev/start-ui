import { Tabs } from "nextra/components";
import DateRangePicker from "@start-base/react-form-elements/DateRangePicker";
import "@start-base/react-form-elements/styles.css";
import "react-day-picker/dist/style.css";
import Preview from "@components/Preview/Preview";
import ApiReferenceButton from "@components/ApiReferenceButton/ApiReferenceButton";
import FormOnChange from "./formOnChange";
import { Steps } from "nextra/components";
import "@start-base/react-form-elements/styles.css";

# Date Picker

The DatePicker component is a user-friendly and customizable calendar input designed for selecting a single date.

<ApiReferenceButton path="form-daterangepicker--docs" />

## Installation

<Steps>

### Install the package

<Tabs items={["npm", "yarn", "pnpm"]}>
  <Tabs.Tab>
    ``` bash copy npm install --save @start-base/react-form-elements ```
  </Tabs.Tab>
  <Tabs.Tab>
    ``` bash copy yarn add @start-base/react-form-elements ```
  </Tabs.Tab>
  <Tabs.Tab>
    ``` bash copy pnpm add @start-base/react-form-elements ```
  </Tabs.Tab>
</Tabs>
### Add the styles Make sure to add css file to your app root file

```tsx copy
import "@start-base/react-form-elements/styles.css";
```

</Steps>

<Tabs items={["Preview", "Code"]}>
  <Tabs.Tab>
  <Preview height="800px">
  <FormOnChange>
      <DateRangePicker
    label="Date Range Picker"
    name="input"
  />
  </FormOnChange>
  </Preview>
  </Tabs.Tab>
  <Tabs.Tab>
    ```ts copy
    import DatePicker from '@start-base/react-form-elements/DatePicker';
    import '@start-base/react-form-elements/styles.css';

    <DatePicker
        label="DateRangePicker"
        name="input"
        onChange={handleChange}
    />
    ```

  </Tabs.Tab>
</Tabs>
