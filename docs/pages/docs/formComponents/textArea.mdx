import { Tabs } from 'nextra/components';
import '@start-base/react-form-elements/styles.css';
import TextArea from '@start-base/react-form-elements/TextArea';
import Preview from '@components/Preview/Preview';
import FormOnChange from '@components/formOnChange';
import { Steps } from 'nextra/components';

# TextArea

The TextArea component is designed for multi-line text input, providing users with an area to enter larger amounts of text. It features a flexible and resizable interface that adapts to user input

## Installation

<Steps>
    ### Install the package
    ```sh copy npm2yarn
    npm i @start-base/react-form-elements
    ```
    ### Add the styles

    <Tabs items={["Next.js 14 (App Directory)", "Next.js 13", "React.js"]}>
        <Tabs.Tab label="Next.js 14 (App Directory)">
            To use the React form elements styles in a Next.js 14 project, import the styles in your `layout.tsx` file.

            ```tsx copy filename="app/layout.tsx"
            import "@start-base/react-form-elements/styles.css";
            ```

        </Tabs.Tab>
        <Tabs.Tab label="Next.js 13">
            For Next.js 13 projects, ensure you include the React form elements styles in the `_app.tsx` file.

            ```tsx copy filename="pages/_app.tsx"
            import "@start-base/react-form-elements/styles.css";
            ```

        </Tabs.Tab>
        <Tabs.Tab label="React.js">
            In a React.js project, you should import the styles directly into your `App.js` file.

            ```tsx copy filename="App.js"
            import "@start-base/react-form-elements/styles.css";
            ```

        </Tabs.Tab>

    </Tabs>

    ### Import the component

    <Tabs items={["Individual", "Global"]}>
        <Tabs.Tab label="Individual">

            ```tsx copy
            import TextArea from '@start-base/react-form-elements/TextArea';
            ```

        </Tabs.Tab>
        <Tabs.Tab label="Global">

            ```tsx copy
            import { TextArea } from '@start-base/react-form-elements';
            ```

        </Tabs.Tab>

    </Tabs>

</Steps>

## Usages

<Tabs items={["Preview", "Code", "React Hook Form"]}>
  <Tabs.Tab>
  <Preview height="200px">
   
  <FormOnChange>
    <TextArea
    name="input"
  />
  </FormOnChange>
  </Preview>
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <TextArea
        name="input"
        onChange={handleChange}
    />
    ```

  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <Controller
        name="input"
        control={control}
        render={({ field }) => (
          <TextArea
            value={watch("input")}
            {...field}
          />
        )}
    />
    ```
  </Tabs.Tab>
</Tabs>

### Text Area Component Label

<Tabs items={['Preview', 'Code', 'React Hook Form']}>
  <Tabs.Tab>
    <Preview height="200px">
      <FormOnChange>
        <TextArea label="TextArea Label" name="input" />
      </FormOnChange>
    </Preview>
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <TextArea label="TextArea Label" name="input" onChange={handleChange} />
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <Controller
        name="input"
        control={control}
        render={({ field }) => (
          <TextArea
            value={watch("input")}
            {...field}
          />
        )}
    />
    ```
  </Tabs.Tab>
</Tabs>

### Text Area Component Placeholder

<Tabs items={['Preview', 'Code', 'React Hook Form']}>
  <Tabs.Tab>
    <Preview height="200px">
      <FormOnChange>
        <TextArea placeholder="TextArea Placeholder" name="input" />
      </FormOnChange>
    </Preview>
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <TextArea
      placeholder="TextArea Placeholder"
      name="input"
      onChange={handleChange}
    />
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <Controller
        name="input"
        control={control}
        render={({ field }) => (
          <TextArea
            value={watch("input")}
            {...field}
          />
        )}
    />
    ```
  </Tabs.Tab>
</Tabs>

### Text Area Component Placeholder And Label

<Tabs items={['Preview', 'Code', 'React Hook Form']}>
  <Tabs.Tab>
    <Preview height="200px">
      <FormOnChange>
        <TextArea
          label="TextArea Label"
          placeholder="TextArea Placeholder"
          name="input"
        />
      </FormOnChange>
    </Preview>
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <TextArea
      label="TextArea Label"
      placeholder="TextArea Placeholder"
      name="input"
      onChange={handleChange}
    />
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <Controller
        name="input"
        control={control}
        render={({ field }) => (
          <TextArea
            value={watch("input")}
            {...field}
          />
        )}
    />
    ```
  </Tabs.Tab>
</Tabs>

### Text Area Component Resizable

<Tabs items={['Preview', 'Code', 'React Hook Form']}>
  <Tabs.Tab>
    <Preview height="200px">
      <FormOnChange>
        <TextArea
          autoGrow
          label="TextArea Label"
          placeholder="TextArea Placeholder"
          name="input"
        />
      </FormOnChange>
    </Preview>
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <TextArea
      autoGrow
      label="TextArea Label"
      placeholder="TextArea Placeholder"
      name="input"
      onChange={handleChange}
    />
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <Controller
        name="input"
        control={control}
        render={({ field }) => (
          <TextArea
            value={watch("input")}
            {...field}
          />
        )}
    />
    ```
  </Tabs.Tab>
</Tabs>

### Text Area Component Disable Shrink

<Tabs items={['Preview', 'Code', 'React Hook Form']}>
  <Tabs.Tab>
    <Preview height="200px">
      <FormOnChange>
        <TextArea
          disableShrink
          label="TextArea Label"
          placeholder="TextArea Placeholder"
          name="input"
        />
      </FormOnChange>
    </Preview>
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <TextArea
      disableShrink
      label="TextArea Label"
      placeholder="TextArea Placeholder"
      name="input"
      onChange={handleChange}
    />
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <Controller
        name="input"
        control={control}
        render={({ field }) => (
          <TextArea
            value={watch("input")}
            {...field}
          />
        )}
    />
    ```
  </Tabs.Tab>
</Tabs>

### Text Area Component Error

<Tabs items={['Preview', 'Code', 'React Hook Form']}>
  <Tabs.Tab>
    <Preview height="200px">
      <FormOnChange>
        <TextArea error="Error Message" label="TextArea Label" name="input" />
      </FormOnChange>
    </Preview>
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <TextArea
      error="Error Message"
      label="TextArea Label"
      name="input"
      onChange={handleChange}
    />
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```jsx copy showLineNumbers
    <Controller
        name="input"
        control={control}
        render={({ field }) => (
          <TextArea
            value={watch("input")}
            {...field}
          />
        )}
    />
    ```
  </Tabs.Tab>
</Tabs>

## API

A textarea component with optional auto-grow functionality.

| Name           | Type                              | Default | Description                                                                                                                                    |
| -------------- | --------------------------------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| name           | `string`                          |         | Name of the input                                                                                                                              |
| label          | `string`                          | `null`  | Label of the input                                                                                                                             |
| placeholder    | `string`                          | `null`  | Placeholder of the input                                                                                                                       |
| disableShrink  | `boolean`                         | `false` | Position the label outside of the input                                                                                                        |
| value          | `string` or `number`              |         | Value of the input                                                                                                                             |
| onChange       | `function`                        |         | Callback function that is fired when the value changes.                                                                                        |
| inputClassName | `string`                          | `null`  | Classname for input element                                                                                                                    |
| labelClassName | `string`                          | `null`  | Classname for label element                                                                                                                    |
| errorClassName | `string`                          | `null`  | Classname for error element                                                                                                                    |
| disabled       | `boolean`                         | `false` | Disable the input                                                                                                                              |
| error          | `string` or `boolean` or `object` | `null`  | Error message to display below the input. if value is a boolean input border will be change. Also you can pass schema validator error objects. |
| autoGrow       | `boolean`                         | `false` | Auto grow textarea                                                                                                                             |

## Styling
